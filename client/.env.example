# Base URL for the backend API. Every request in src/api/ is built from this
# single value — never hardcode a URL anywhere else in the app.
# The (untouched) auth pages still hardcode http://localhost:5000/api/v1
# directly; keep this pointed at the same host during local development.
VITE_API_URL=http://localhost:5000
